<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/page_overview"
        android:enabled="true"
        android:icon="@drawable/ic_splash_rhombus"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/page_list"
        android:enabled="true"
        android:icon="@drawable/ic_splash_list"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/page_bookmark"
        android:enabled="true"
        android:icon="@drawable/ic_splash_bookmark"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/page_profile"
        android:enabled="true"
        android:icon="@drawable/ic_splash_profile"
        tools:ignore="MenuTitle" />
</menu>